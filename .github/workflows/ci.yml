name: CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install
        run: bun install
      - name: Test
        run: bun test
      - name: Build
        run: bun run build
