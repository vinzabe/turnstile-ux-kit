name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install
        run: bun install

      - name: Test
        run: bun test

      - name: Lint
        run: bunx eslint src tests --ext .ts,.tsx || true

      - name: Format
        run: bunx prettier --check "src/**/*.{ts,tsx,json,css}" "tests/**/*.{ts,tsx}" || true

      - name: Build
        run: bun run build
